# Dipendenze per renderizzare la dashboard (dashboard/index.qmd).
# La pipeline ha le sue nel requirements.txt di root: il render non ne ha bisogno,
# legge i CSV gia' scritti in dashboard/data/.
altair>=6.1
pandas>=3.0
vl-convert-python>=1.7   # export PNG dei grafici per le slide

# Shim del kernel di Quarto: li importa dallo stesso interprete che lancia.
# In locale arrivano per transitivita', su un runner pulito no, e il render muore
# con "No module named 'yaml'".
ipykernel>=7.3
jupyter-client>=8.9
nbclient>=0.11
nbformat>=5.10
pyyaml>=6.0
