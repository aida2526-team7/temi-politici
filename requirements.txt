# Discovery
mediacloud            # Media Cloud client (mediacloud_spike.py)
requests              # HTTP (harvester.py)

# Full text (harvester.py)
beautifulsoup4        # HTML parsing
trafilatura           # boilerplate removal (falls back to <p> if missing)
langdetect            # per-article language
pypdf                 # testo dei PDF (programmi elettorali: trafilatura fa solo HTML)

# Topic-modeling baseline (notebooks/classificatore.ipynb)
pandas                # JSONL and tabular exports
scikit-learn          # TF-IDF + NMF
python-dotenv         # optional local .env for MC_API_KEY
